// Owner: Track 4 (Whiteboard, notes, and supporting modules)
// Responsible for: shared design tokens (colors, spacing, typography) applied consistently across Kanban, whiteboard, and notes pages.

// getDesignTokens returns the app-wide token set consumed by the CSS framework/styling setup.
function getDesignTokens(): Record<string, string> {
  // TODO: define the color palette, spacing scale, and typography tokens the team agrees on in week 2
  return {};
}
