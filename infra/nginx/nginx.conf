# Owner: Track 1 (Foundation, Auth, and API infrastructure)
# Responsible for: HTTPS termination and reverse proxying all browser-facing traffic to the frontend/backend containers, per the mandatory HTTPS requirement.

# TODO: configure a server block on :443 with a TLS certificate, redirecting :80 -> :443
# TODO: proxy_pass /api/* and /ws (WebSocket upgrade headers) to the Go backend container
# TODO: proxy_pass / to the frontend static build/dev server container
# TODO: internal container-to-container traffic (backend <-> Postgres) does not need TLS per the general requirements
